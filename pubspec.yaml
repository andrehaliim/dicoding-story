name: story
description: "Dicoding Submission for Intermediate Flutter Class"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.5.4
  geocoding: ^4.0.0
  intl: ^0.20.2
  image_picker: ^1.2.1
  geolocator: ^14.0.2
  image: ^4.8.0
  provider: ^6.1.2
  flutter_localizations:
    sdk: flutter
  google_maps_flutter: ^2.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.0

flutter:
  uses-material-design: true
